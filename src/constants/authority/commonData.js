
// * ~ CUSTOME APP CONFIG ~ 

const COLLECTION = {};

// Routes
COLLECTION.ROUTE = {
  login: '/user/login',
  welcome: '/app/welcome',
};

// Idle
COLLECTION.IDLE = {
  timeout: 1000 * 60 * 10,
};

export default COLLECTION;
